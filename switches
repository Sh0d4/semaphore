- hosts: ios_switches
  gather_facts: no
  tasks:
    - name: Testar conexão com switches IOS
      ios_command:
        commands:
          - show version
      register: result

    - name: Mostrar saída
      debug:
        var: result.stdout_lines
